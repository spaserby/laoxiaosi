# 统一换行符：仓库内一律 LF（Windows 开发 / Linux 部署/容器 之间不再互相污染）
# 踩坑记录：CRLF 的 .env 与 shell 脚本在 Linux 上会解析失败（域名/脚本莫名报错）
* text=auto eol=lf

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.jar binary
*.woff binary
*.woff2 binary

# Windows 专用脚本保留 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
